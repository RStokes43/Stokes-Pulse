__pycache__/
*.pyc
.venv/
venv/

# runtime state / secrets — never tracked
config/alerting.json
config/secrets.json
config/maintenance.json
config/alert_overrides.json
config/allowed_emails.json
config/oauth.json
config/secret_key
data/

# Android build output — the APK GitHub Actions builds is committed
# separately to android/dist/, everything else here is regenerable.
android/.gradle/
android/app/build/
android/build/
android/local.properties
